apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mass-generator
  labels:
    app: mass-generator
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: mass-generator
    app.kubernetes.io/instance: {{ .Release.Name }}
