# Per-Feature RC Deployments — Technical Reference

> **This document has been superseded.**
>
> The previous per-client approach (tag-triggered matrix deployments for `apply` and `decide`
> clients using `rc-apply.yaml` / `rc-decide.yaml` values files) has been replaced by
> per-feature environments triggered via `workflow_dispatch`.
>
> The full technical reference is now in:
> **[`feature-environments.md`](./feature-environments.md)**
>
> The strategy rationale is in:
> **[`per-client-integration.md`](./per-client-integration.md)**
