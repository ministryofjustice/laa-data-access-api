{{/*
service.yaml
Creates a Kubernetes Service to expose the application pods.
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "providers-app.fullname" . }}
  labels:
    {{- include "providers-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "providers-app.selectorLabels" . | nindent 4 }}
