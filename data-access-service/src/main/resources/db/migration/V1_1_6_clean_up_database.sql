-- Drop status lookup table if it still exists
DROP TABLE IF EXISTS status_code_lookup CASCADE;

-- Drop application proceedings tables if they exist
DROP TABLE IF EXISTS applications_proceedings CASCADE;
