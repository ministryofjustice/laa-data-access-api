package uk.gov.justice.laa.dstew.access.command.application.linkedgroup.route;

/** Durable write-side membership state for an application's linked-group route. */
public enum ApplicationGroupRouteKind {
  STANDALONE,
  LINKED_GROUP
}
